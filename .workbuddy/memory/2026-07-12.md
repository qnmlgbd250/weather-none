# 2026-07-12

## 死代码排查（SkyPulse 天气 App）
- 拉取最新代码（HEAD 6b3a1c4），作为安卓架构师排查死代码，仅列结果未修改。
- 高价值发现：
  - `ui/components/CustomPullRefreshIndicator.kt` 整文件死代码（下拉刷新已改 pullRefresh + WeatherContentBody）。
  - `ui/screen/WeatherContent.kt` 整文件死代码（被 WeatherScreen 内的 WeatherContentBody 取代，重复实现）。
  - `HourlyForecast.kt` 的 `windColor()` 死函数；`WeatherWidgetProvider.kt` 的 `ONETIME_THROTTLE_MS` 死常量；`RefreshManager.kt` 未用的 `locationManager` 注入；`GlassCard.kt` 未用的 `GlassCardBg()`。
  - 死资源：`ic_launcher_round` 全密度、`drawable/ic_launcher_foreground.xml`（与 mipmap PNG 重复）。
  - 31 处未使用 import（已逐文件核实零实际使用）。
- 已排除误报：Hilt 模块、*Test 类、skyPulseScreenTransition()（L194 实际调用）、cityMutexes、getValue/setValue 委托。
- 报告产出：`DEAD_CODE_REPORT.md`（工作区根目录）。
- 方法：用 Python 脚本全量扫描引用 + 逐文件 grep 核实，避免误报。
