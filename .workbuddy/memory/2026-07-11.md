# 2026-07-11

## 清理 T_MAP_KEY 死代码链路
- 排查结论：`T_MAP_KEY` 是死代码，仅存在于 `local.properties` → `build.gradle.kts` 读取 → `BuildConfig.T_MAP_KEY`，业务代码（`app/src`）从未读取。其值是业界知名的硬编码泄漏腾讯地图 Key，与本项目（AMAP 定位）无关。
- 已删除：
  1. `local.properties` 第 11 行 `T_MAP_KEY=...`
  2. `build.gradle.kts` 的 `tMapKey`/`escapedTMapKey` 读取与转义（原 32–35 行）
  3. `build.gradle.kts` 的 `buildConfigField("String", "T_MAP_KEY", …)`（原 73 行）
- 验证：`gradlew.bat help` BUILD SUCCESSFUL，确认 build 脚本解析无误，主逻辑（AMAP/CAIYUN/XIAOMI）未受影响。
- 备注：`app/build/generated/.../BuildConfig.java` 仍含旧 `T_MAP_KEY`，属自动生成缓存，下次编译会重新生成且不再包含；非源码问题。
- 待办（可选）：同模板拷入的 `XIAOMI_APP_KEY`/`XIAOMI_SIGN` 可能也是悬空 Key，未处理，需另行确认是否使用。

## 提交并推送到远程
- 提交 `c8c04c1`：`refactor: 移除未使用的 T_MAP_KEY 死代码`（仅 build.gradle.kts，5 行删除）。
- 已 `git push origin main`（43ccbf6..c8c04c1）。`local.properties` 为 gitignore 本地配置，不入库；未做版本号 bump（非发版流程，仅普通代码推送）。
